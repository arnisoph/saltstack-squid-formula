os_family:
  Debian:
    pkgs:
      - squid3
    config:
      main:
        path: /etc/squid3/squid.conf
    service:
      name: squid3
  RedHat:
    pkgs:
      - squid
    config:
      main:
        path: /etc/squid/squid.conf
    service:
      name: squid
